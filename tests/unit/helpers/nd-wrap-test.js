
import { ndWrap } from 'alternatives/helpers/nd-wrap';
import { module, test } from 'qunit';

module('Unit | Helper | nd wrap');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ndWrap([42]);
  assert.ok(result);
});

